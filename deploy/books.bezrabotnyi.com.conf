server {
    listen 80;
    listen [::]:80;
    server_name books.bezrabotnyi.com;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name books.bezrabotnyi.com;

    location / {
        proxy_pass http://192.168.2.75:3000;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_http_version 1.1;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        gzip off;
    }

    ssl_certificate /etc/letsencrypt/live/books.bezrabotnyi.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/books.bezrabotnyi.com/privkey.pem;
}
